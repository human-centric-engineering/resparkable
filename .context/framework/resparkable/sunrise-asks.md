# Sunrise Asks

Things Resparkable needs from **upstream Sunrise** — seams that don't exist yet,
core files a fork is currently forced to edit, and platform gaps that will bite
a later release.

Upstream repo: <https://github.com/human-centric-engineering/sunrise>

## Why this file exists

Resparkable is a framework-tier module, so **every edit to a Sunrise-owned file is a
merge conflict inflicted on every host project** that installs Resparkable. When
Resparkable needs something core doesn't expose, the answer is a seam upstream — not
a local edit. This file is the register of those requests, so they don't quietly
turn into "just this once" edits that make install #2 an archaeology exercise
(risk 1b in [`plan.md`](./plan.md#17-risks-ranked-by-likelihood-of-biting)).

Resparkable is not the only fork doing this. Daybreak, Reclaim Your Week, ConQuest
and HCE Hub all file upstream asks the same way — several of the seams Resparkable
needs were already raised by one of them. **Search before filing.**

## The process

When a Sunrise gap surfaces during Resparkable work:

1. **Search upstream first** — `gh issue list --repo
human-centric-engineering/sunrise --state all --limit 300`. Roughly half of
   Resparkable's first batch turned out to be already filed by another fork. A
   comment on an existing issue with a second fork's use case is worth more than
   a duplicate.
2. **Add a row below** — what's needed, why, and what Resparkable does until it
   lands.
3. **Open an issue** (or comment on the existing one) and put the number in the
   Issue column. A row without an issue is a note to self; an issue is a request
   someone can action.
4. **Record the workaround in [`install.md`](./install.md)** if a host project
   has to do anything differently until the seam lands.
5. When it ships upstream, **remove the workaround, move the row to Landed, and
   note where it landed** — a commit, or a version once one is tagged. Most asks
   land on `main` long before a release, so a version column would read empty
   for weeks and make landed work look pending.
6. **Re-check the states before starting a phase**, with the merge in the same
   breath:

   ```bash
   git fetch upstream && git rev-list --count HEAD..upstream/main
   gh issue list --repo human-centric-engineering/sunrise --state all \
     --limit 300 --json number,state,title
   ```

   This file went stale in exactly that gap: eleven asks were closed upstream
   across three days while this fork sat 90 commits behind, so the register said
   OPEN for work that was already done — including the one blocking phase 6.
   **A closed issue changes nothing here until the merge happens**, which is why
   the two commands belong together.

7. **Re-read the code before filing a row you wrote earlier.** A row is a note
   taken at the moment something bit; the issue is a claim about code as it
   stands. Those drift apart, and twice now the row has been the thing that was
   wrong — ask #13 was filed and retracted the same day, and ask #6 turned out to
   have mis-described the module in two separate ways (see its row). Both would
   have been caught by opening the file first. Budget the re-read into filing,
   not into the apology afterwards.

House style upstream: what the gap is, what it cost a real build, what would
close it, and a "downstream status" note saying what the fork carries locally in
the meantime. State the trust boundary explicitly when the ask touches security —
"no trust boundary crossed" up front is what lets a maintainer triage it as
hardening instead of dropping everything.

---

## Already raised upstream — Resparkable is a second data point

Both of Resparkable's blocking asks were already filed by other forks, so Resparkable's
use case went on the existing threads rather than into duplicate issues.
**Both have since landed** — see [Landed](#landed).

| #   | Ask                                                                                                                               | Upstream                                                                            | Resparkable's angle                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status                                     |
| --- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| 1   | **A seam for an app's own recurring job** — Resparkable wants `registerAppMaintenanceTask({ name, run })` on the maintenance tick | [#469](https://github.com/human-centric-engineering/sunrise/issues/469) CLOSED      | #469 proposed `lib/app/scheduled-jobs.ts` driven by the **scheduler**; Resparkable's connection sweep, retention pass and (Release 4) vault sync want the **maintenance tick** — batch-of-5, `SKIP LOCKED`, 60s budget. Same gap, different knob. [Commented 2026-07-28](https://github.com/human-centric-engineering/sunrise/issues/469#issuecomment-5110230342) with the three continuous per-user sweeps and why a cron field is the wrong shape for them              | **Landed** as `lib/app/jobs.ts`, see below |
| 2   | **`lib/app/protected-nav.ts`** — a protected-nav seam mirroring `lib/app/admin-nav.ts`                                            | [#473](https://github.com/human-centric-engineering/sunrise/issues/473) CLOSED      | Identical gap, found independently. #473 also covers the hardcoded `/dashboard` post-auth destination in eight places, which Resparkable will hit the moment `/resparkable` becomes the product's real home. [Commented 2026-07-28](https://github.com/human-centric-engineering/sunrise/issues/473#issuecomment-5110230474) — for a framework tier the hand-edit multiplies across every host install, and the post-auth destination is the half that actually dead-ends | **Landed**, see below                      |
| 8   | **Per-user scheduling** — `AiWorkflowSchedule` has `createdBy` but no per-user notion                                             | folded into [#469](https://github.com/human-centric-engineering/sunrise/issues/469) | Resparkable rides the maintenance tick instead, which is the right call for per-user sweeps anyway. Low priority                                                                                                                                                                                                                                                                                                                                                          | Closed with #469                           |

---

## Affects Resparkable's design — open upstream, not our ask

| Upstream                                                                                              | What                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Why Resparkable cares                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [#462](https://github.com/human-centric-engineering/sunrise/issues/462) **CLOSED**, landed `93a8180e` | **Boot-registered context contributors and capability handlers are lost at request time under Next 16 + Turbopack.** `instrumentation.ts` runs in a separate module graph from route handlers; the request-path self-heal re-ran core's built-ins and the **app** tier, but not a **framework** tier that registered at boot. Silent — no error, the tools and the context block simply weren't there. Fixed as proposed: both registries are `globalThis`-backed | **This was Resparkable's exact shape, and it was the phase-6 blocker.** Phase 6 registers 14 capabilities and a context contributor from `initResparkable()`; phase 7 adds more. On Turbopack they would have silently vanished at request time. **Phase 6 no longer needs a request-path self-heal of its own** — the decision this row said to take before writing phase 6 has been taken upstream |
| [#367](https://github.com/human-centric-engineering/sunrise/issues/367) OPEN                          | Intra-tenant ownership-scope seam (owner/team-scoped resource visibility)                                                                                                                                                                                                                                                                                                                                                                                         | Adjacent to Resparkable's D5 `OwnerScope`, but not blocking — Resparkable owns its scope boundary inside its own tier. Worth watching in case the platform seam supersedes it                                                                                                                                                                                                                        |
| [#505](https://github.com/human-centric-engineering/sunrise/pull/505) **MERGED** in 0.8.0             | **Multi-tenancy gap analysis** (`.context/architecture/multi-tenancy.md`) — an opt-in RLS retrofit playbook, single-tenant by default behind a `TENANCY_MODE` seam, plus "the four planes RLS can't reach"                                                                                                                                                                                                                                                        | Docs, not code, so nothing to merge into. Read it before Release 2's sharing work: Resparkable's D5 answer is a **branded type at the repo boundary**, which is one of the planes the doc argues RLS does not cover. Confirms the tier keeps owning its own boundary rather than waiting for a platform one                                                                                          |
| [#429](https://github.com/human-centric-engineering/sunrise/issues/429) **CLOSED**                    | `prisma/schema/app.prisma` ships platform models despite docs implying it's fork-reserved                                                                                                                                                                                                                                                                                                                                                                         | The `/framework` tier Resparkable uses **is** genuinely reserved, so Resparkable was unaffected. Re-read the leaf-tier advice in `install.md` against however it was resolved                                                                                                                                                                                                                        |
| [#442](https://github.com/human-centric-engineering/sunrise/issues/442) **CLOSED**                    | Maintenance tick did unconditional DB work every 60s                                                                                                                                                                                                                                                                                                                                                                                                              | Fixed by giving each background task a **minimum interval** and letting an idle tick do zero database work. Resparkable's phase-7 jobs sit on that same tick and inherit the shape — `registerAppJob({ intervalMs })` is the knob, and `intervalMs` is a floor rather than a promise                                                                                                                 |

---

## Resparkable's own asks — checked against the 88 existing issues, no duplicates

| #   | Ask                                                                                                                                                                                                                                                                                                                                              | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Priority                                                                                                                                                                                                                                                                                           | Issue                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 4   | **`lib/app/env.ts` can't honour §4's dynamic-import rule, and doesn't say so** — a doc note, not a mechanism                                                                                                                                                                                                                                     | Filed as docs-only after re-reading. **The row's "contradicting the dynamic-import rule" framing was wrong**: §4 scopes that rule to `lib/app/bootstrap.ts` alone, so there is no contradiction. The real point is that the _property_ the rule protects — a static `@/lib/framework/...` specifier is resolved at `next build` and breaks any fork without that folder — **cannot be preserved** at `lib/app/env.ts`, because `lib/env.ts` reads the schema during a synchronous module-load parse. And `registerEnvSchema()` turns out not to be buildable: registration would have to precede a parse that everything is downstream of, and deferring it would give up fail-fast-at-boot, which is worth more. So the ask is one paragraph in the seam's header, and the issue says so                                                                                                                                                                                                                                                                                                                                                                                          | Low — docs only. The seam works correctly today; this saves the next framework-tier fork the same twenty minutes                                                                                                                                                                                   | [#535](https://github.com/human-centric-engineering/sunrise/issues/535)                              |
| 5   | **Admin/avatar test flake under parallel load**                                                                                                                                                                                                                                                                                                  | Observed across **four** full-suite runs on 2026-07-28/29, one machine: 1–5 tests fail in **different** files each time, all `waitFor` timeouts in admin/orchestration UI tests, all passing in isolation. `new-page.test.tsx` has now appeared twice. The fourth run had nothing else running, which removes the "loaded laptop" explanation for that one — but it is still a single machine. **Check CI history before filing**; if CI shows the same churn, this is worth an issue with the run links                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Hold** — needs CI evidence                                                                                                                                                                                                                                                                       | _not filed_                                                                                          |
| 6   | **`safe-url.ts` names a compensating control that doesn't compensate** (`lib/security/safe-url.ts`) — correct the header, re-check on redirect, and treat DNS resolution as a separate decision                                                                                                                                                  | **This row was wrong as originally written, in two ways, and the correction is the useful part.** It said the guard "does no DNS resolution, so a hostname resolving to a private address passes… the gap affects every outbound fetch". Re-reading the module before filing: the DNS omission is **deliberate and documented**, with a written rationale — so "they forgot" was never the finding. And it does **not** affect every outbound fetch: the workflow `external_call` executor uses an explicit **host allowlist**, which is stronger than this guard and immune to the whole class. What is genuinely wrong is narrower and sharper: the header cites `provider-manager.buildProviderFromConfig` as blocking private ranges "at fetch-time too", but that call is `checkSafeProviderUrl(config.baseUrl)` — **the same string check on the same string**, which adds nothing against DNS. Separately, two callers validate once then follow redirects: `knowledge/url-fetcher.ts` (`redirect: 'follow'`, ingests the body) and `hooks/registry.ts:233` (no `redirect` option, so it defaults to follow, and **POSTs** the payload plus HMAC headers wherever it lands) | Low — **no trust boundary crossed.** Every path is `withAdminAuth`, and an admin can already set a provider `baseUrl`. Filed as hardening plus a doc correction, on the same reasoning as #23/#509: the guard reads as stronger than it is                                                         | [#534](https://github.com/human-centric-engineering/sunrise/issues/534)                              |
| 7   | **Decide who owns per-user credential storage** — an AES-256-GCM `secret-box` with optional env keys, fail-closed                                                                                                                                                                                                                                | No encryption utility exists anywhere (zero hits for `createCipheriv\|aes-256\|kms`), and `AiWorkflowTrigger.signingSecret` is plaintext under a documented "the DB is admin-trusted" posture that does not survive user-owned third-party tokens. **Needs a decision, not necessarily code** — if Sunrise doesn't want it, Resparkable builds it in its own tier                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | File when Release 4 is real                                                                                                                                                                                                                                                                        | _not filed_                                                                                          |
| 9   | **Reconcile the two upload caps** — 5 MB global (`lib/validations/storage.ts:15`) vs the bulk knowledge route's local 50 MB / 10 files                                                                                                                                                                                                           | Resparkable's document ingestion (phase 4) had to pick one. Resolved downstream by making it an operator setting (`ResparkableSettings.maxDocumentBytes`, default 25 MB) rather than choosing for them — but a fork shouldn't have to build a settings screen to resolve an upstream contradiction. Minor                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Note only                                                                                                                                                                                                                                                                                          | _not filed_                                                                                          |
| 17  | **Export the `nullOnMiss` / P2025 convention as a documented helper, or note the `update` vs `updateMany` trap** — a scoped `prisma.update({ where: { id, userId } })` throws `P2025` when the row is not the caller's, which is correct for a single write and a **footgun inside `$transaction`**: one vanished row rolls back the whole batch | Cost a real bug in Resparkable phase 4. `stampIndexedHash` batched six delegates' `update` calls into one transaction _after_ paying for embeddings; a row deleted mid-pass would have rolled back every stamp and failed the request with the money already spent. `updateMany` is the right primitive there (a miss is 0 rows, not a throw), but nothing in the platform docs draws the distinction                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Low — a docs/ergonomics gap, not a defect. **This row held up on re-read**, and gained one supporting fact: core has 25 `$transaction` call sites and **none** use the array form, so a fork writing its first batch has no in-repo example to pattern-match and no counter-example warning it off | [#536](https://github.com/human-centric-engineering/sunrise/issues/536)                              |
| 13  | ~~**Three tests fail on a clean `main`**~~ — **WITHDRAWN, filed in error**                                                                                                                                                                                                                                                                       | Filed then retracted the same day. `bootstrap-wiring.test.ts` (2) and `executor-registry.test.ts` (1) failed on `Test timed out in 10000ms` three consecutive times, including at the fork point `e228c204` in a clean worktree — then passed in a full-suite run (`1010 files, 20,459 tests, 0 failures, 219s`) and passed again on the exact reproduction command. **The lesson is the one worth keeping: "re-run it in isolation" does not distinguish a real failure from load sensitivity if the machine is still saturated.** All three checks ran in the window right after a 39-minute `--coverage` run; a quiet shell is not an idle machine. This was ask #5's load sensitivity all along, not a second phenomenon. Two real observations survive: `--coverage` costs **10×** wall-clock here (2340s vs 219s), and it writes **no report at all** when any test fails, so a failing run yields neither green tests nor coverage data                                                                                                                                                                                                                                     | [Corrected upstream 2026-07-29](https://github.com/human-centric-engineering/sunrise/issues/484#issuecomment-5116696249) with the full picture and an offer to close. Do not re-file without evidence from a machine known to be idle, or from CI                                                  | [#484](https://github.com/human-centric-engineering/sunrise/issues/484) **CLOSED** by us, 2026-07-29 |

### Found by the 2026-08-02 pre-merge gate run

Four findings from the security pass over the `chore/merge-upstream-sunrise`
branch. Only #20 has an exploit path; the other three are sharp edges that are
currently unreachable, and are filed so they stay that way.

| #   | Ask                                                                                                                                                                                                       | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Priority                                                                                                          | Issue                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 20  | **`isRootRelativePath` misses the tab/LF/CR bypass** (`lib/security/sanitize.ts`) — apply the same WHATWG normalisation `sanitizeUrl` already got, and return the normalised value from `safeCallbackUrl` | The guard judges `path[1]` on the raw string, but the WHATWG parser removes tab/LF/CR from **anywhere** before reading the authority, and `trim()` only reaches the ends. So `/<TAB>/evil.com` passes both `startsWith('/')` and the `path[1]` test, then collapses to `//evil.com`. Reachable: `?callbackUrl=/%09/evil.com` → `login-form.tsx:38` → `router.push` at `:127` → Next hard-navigates off-origin, i.e. a phishing landing straight off a genuine successful login. **Pre-existing** — `main`'s `!startsWith('//')` had the same hole — but #473 extracted, exported and documented the function as "safe to redirect to same-origin", so the hardening is incomplete rather than merely inherited. OAuth is unaffected (better-auth's own allowlist rejects the tab). Note the fix must be tab/LF/CR only: reusing `URL_NORMALIZE_STRIP` would strip the space and corrupt `/search?q=two words` | **Highest of the four** — the only one with an exploit path. Resparkable is carrying a local patch until it lands | [#506](https://github.com/human-centric-engineering/sunrise/issues/506) |
| 21  | **Domain-separate the two token schemes** — add a `typ` field to the payloads in `lib/storage/access-tokens.ts` and `lib/orchestration/approval-tokens.ts`                                                | Both sign with `BETTER_AUTH_SECRET`, both use `<b64url-payload>.<b64url-sig>`, both call `sign(payloadJson)` with no scheme tag. A signature minted by one verifies structurally as the other; cross-protocol replay fails **only** because the two Zod payload schemas happen to be disjoint on required fields (`key` vs `executionId`). That is safe by accident of the schemas, not by design — the day either payload gains an optional field that makes it parse as the other, a storage token becomes an approval token                                                                                                                                                                                                                                                                                                                                                                                | Cheap to fix, and the failure mode is silent                                                                      | [#507](https://github.com/human-centric-engineering/sunrise/issues/507) |
| 22  | **`LocalProvider.deletePrefix` should refuse a root-equivalent prefix** (`lib/storage/providers/local.ts`)                                                                                                | Prefix `"."` passes `validateStorageKey`, and `resolveWithin` permits `fullPath === rootPath` — so the call recursively deletes the entire storage root. Not reachable today: both callers pass a session-derived `avatars/${userId}/` (`lib/privacy/erase-user.ts`, `app/api/v1/users/me/avatar/route.ts`). It is one careless caller away from being catastrophic, and the guard is a single comparison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Defence-in-depth, but the blast radius is total                                                                   | [#508](https://github.com/human-centric-engineering/sunrise/issues/508) |
| 23  | **Require `functionDefinition.name === slug`** on capability create/update (`lib/validations/orchestration.ts`)                                                                                           | The chat handler's new tool-call guard keys on `functionDefinition.name`, while dispatch treats that same string as the **slug**, and no schema requires the two to match. A capability with `slug: 'estimate_workflow_cost'` and `name: 'apply_audit_changes'` bound to a low-privilege agent would pass the guard and dispatch the privileged built-in. Requires admin write to `AiCapability`, so it crosses no trust boundary — but the guard reads as stronger than it is, and a `.refine()` closes it                                                                                                                                                                                                                                                                                                                                                                                                   | Low — no trust boundary crossed, but it silently weakens a control that landed as a security fix (#476)           | [#509](https://github.com/human-centric-engineering/sunrise/issues/509) |
| 24  | **Fold the Prisma schema format check into `npm run validate`** — it lives only in `ci.yml`, and Prettier has no `.prisma` parser                                                                         | No local command catches schema drift from the pinned Prisma's own formatter, so a fork gets a green `validate` on a branch CI rejects. Cost a red CI run here: the dependency sweep took `@prisma/client` 7.8.0 → 7.9.1, whose formatter changed field alignment and block-attribute ordering, and CI failed on `framework-resparkable.prisma` — a file the branch never touched, after a local gate run that was green on validate, 22,799 tests and 15/15 drift probes. Upstream hit the identical thing in #482 and fixed only its own file. **Lands hardest on forks**: `framework-*.prisma` and `app.prisma` are exactly the files core never reformats, because core never edits them                                                                                                                                                                                                                  | Low effort, and it removes a whole class of fork-only CI surprise                                                 | [#510](https://github.com/human-centric-engineering/sunrise/issues/510) |

**All four landed in `v0.9.0`** (2026-08-19 merge) — see [Landed](#landed).
#506's local patch in `lib/security/sanitize.ts` is removed; #507, #508 and
#509 needed no local patch, only the ask. #510's own fold-in is confirmed:
`npm run validate` now runs `format:prisma:check` and this merge's schema is
clean.

### Found by phase 6b

| #   | Ask                                                                                                                                                                                                            | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority                                                                                                                            | Issue                                                                   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 25  | **Stub `initAppCapabilities()` in `tests/unit/lib/orchestration/capabilities/registry.test.ts`** — one `vi.mock('@/lib/app/capabilities')`, so the built-in counts measure core rather than core plus the fork | `registerBuiltInCapabilities()` calls the fork-owned scaffold every downstream project is _expected_ to fill, and the suite then asserts `toHaveBeenCalledTimes(13)`. Resparkable registers fourteen of its own through the documented seam, so the test fails at 27 — reporting an idempotency failure for a registry that was perfectly idempotent. **Any** fork that uses §4 of `CUSTOMIZATION.md` hits it, and the failure names the wrong thing, so the first instinct is to go looking in the fork's registration code. The fix is one line and the test gets stricter, not looser: it stops depending on a file core promises never to change | Low effort, high confusion cost — it is the first red test a fork sees after wiring capabilities, and it points away from the cause | [#525](https://github.com/human-centric-engineering/sunrise/issues/525) |

**Same family as [#480](https://github.com/human-centric-engineering/sunrise/issues/480)**
("`defaults.test.ts` locks every `lib/app` seam empty, so filling one — the point
of the seams — breaks a core test"), which closed as COMPLETED on 2026-07-30 by
documenting the expected adjustment. `registry.test.ts` was not in that sweep —
reasonably, since the capability seam was the one no fork had filled yet. #525
was therefore filed as a follow-up referencing it rather than as a fresh
complaint, and notes the general shape: _a core test that measures something a
`lib/app/*` seam contributes to_.

**Downstream status:** carried as a local patch to the Sunrise-owned test file,
with a comment pointing at this row. Remove it when the fix lands upstream.

### Found by phase 6c

| #   | Ask                                                                                                                                                                                                               | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Priority                                                                                                                                                  | Issue                                                                   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 26  | **`<ChatInterface>` should accept the stream endpoint as a prop** (`components/admin/orchestration/chat/chat-interface.tsx`) — one optional `streamEndpoint`, defaulting to `API.ADMIN.ORCHESTRATION.CHAT_STREAM` | The component's own header calls it "reusable" and it is, except for one line: `fetch(API.ADMIN.ORCHESTRATION.CHAT_STREAM, …)` is a hardcoded constant with no prop. Any surface that is not admin — a consumer chat page, an app-owned route that needs `contextType`/`contextId`, an embed — cannot use it without editing a Sunrise-owned file. Resparkable needed exactly that for `/resparkable/chat` and built its own instead. The rebuild was the right call in the end (the admin component also renders per-turn cost, token breakdowns and a tool-argument trace strip, none of which belong on a personal brain) but the _reason_ for it should have been a design choice rather than a hardcoded URL | Low effort. Worth pairing with a note in the header about which props are admin-only, so the next fork knows what to turn off rather than what to rebuild | [#526](https://github.com/human-centric-engineering/sunrise/issues/526) |

### Found by phase 7

| #   | Ask                                                                                                                                                                                                                                                                                    | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Priority                                                                                                                                                                                                                             | Issue                                                                   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- |
| 27  | **`CAPABILITY_BINDING_MODE=strict` silently breaks every workflow `tool_call` step, with no operator remedy** — exempt the `workflow:` agent-id prefix, carry the grant on the workflow definition instead, or document the trade-off alongside the `mcp-system` warning already there | `executors/tool-call.ts:102` dispatches with a synthetic `agentId: \`workflow:${ctx.workflowId}\``, and there is no `AiAgentCapability`row for such an id — nor could there be, since it is not an agent. Under`permissive`(the default) the dispatcher synthesizes a default-allow binding and everything works. Under`strict`, `getAgentBinding`returns`null`and **every tool_call step in every workflow fails** with`capability_disabled_for_agent`. The env description already warns that strict "retroactively revokes every capability an agent relied on implicitly, **including the mcp-system agent**" — the workflow path is the same shape and is not mentioned, so an operator auditing `AiAgentCapability`before flipping the flag has no row to find and no way to know. Resparkable phase 7 is five workflows built almost entirely from`tool_call` steps, so for this fork strict mode is currently unusable | Low effort either way; the failure is total-but-silent for anyone who enables strict, and **no binding row can fix it** — `AiAgentCapability.agentId` is an FK to `AiAgent.id`, so a `workflow:<cuid>` row cannot be inserted at all | [#528](https://github.com/human-centric-engineering/sunrise/issues/528) |

**Downstream status:** nothing carried locally — Resparkable relies on the
`permissive` default, like every other install. The ask is to stop strict mode
being a trap rather than to change Resparkable.

**The distinction that makes this worth filing separately** from the
`mcp-system` case the env description already warns about: `mcp-system` is a real
seeded `AiAgent` row, so "audit `AiAgentCapability` before enabling strict" is
advice an operator can act on. `workflow:<cuid>` is not an agent id, and
`AiAgentCapability.agentId` is an FK to `AiAgent.id` — so the remedy the warning
implies **cannot be applied** to the workflow path at all.

### Found by running the app after the 0.8.0 merge (2026-08-05)

The only ask in this file found by **starting the server** rather than by
reading a diff or running a suite — and it could not have been found any other
way.

| #   | Ask                                                                                                                                                                        | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Priority                                                                                                                                                                             | Issue                                                                   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- |
| 31  | **`tool-call.ts` dispatches without ensuring capability registration** — add `registerBuiltInCapabilities()` before the dispatch, matching its sibling `agent-call.ts:484` | #462 made `capabilityDispatcher` a `globalThis` singleton so registrations cross module realms — but the "have I registered yet" guards (`registry.ts:78-80`) are ordinary module-scoped booleans, so the registry is only filled when something **calls** the initialiser. Chat, MCP, `getCapabilityDefinitions()` and the `agent_call` executor all do; `tool-call.ts` does not. So on a process that has served no request, the scheduler firing a workflow of `tool_call` steps dispatches into an empty map. Resparkable's four background workflows are almost entirely `tool_call` steps, so **all four fail at 03:15 and 04:30 on a server that has been quiet overnight — the failure is likeliest exactly when it matters, and hides under load.** It also presents as a fork bug: `unknown_capability` names the fork's own slug, so the first instinct is to go looking at registration code that is working fine (same shape as #525) | Low effort, high blast radius. **No test suite can see it** — unit tests register explicitly, so the registry is never empty; it needs a cold process plus a tick before any request | [#537](https://github.com/human-centric-engineering/sunrise/issues/537) |

**Downstream status (#31):** fixed in the tier, no core edit.
`initResparkable()` calls `registerBuiltInCapabilities()` at boot; because the
registry is `globalThis`-backed the registration persists into the scheduler's
realm, and the call pulls in core's built-ins **and** Resparkable's own (via
`initAppCapabilities()` on the way through), so it is complete rather than half.
Covered by a regression test in `scaffold.test.ts` that asserts the registry
_contains a known slug_ after boot rather than that a function was called — the
failure mode is an empty registry, and only a lookup proves it isn't. Verified to
fail without the fix and pass with it. **#537 landed in `v0.9.0`** — the tier's
call and `scaffold.test.ts` regression are now redundant, harmless dead weight
at boot rather than load-bearing. Not removed yet; safe to drop next time this
file is touched.

**The lesson, which is the reason this row is worth reading twice.** Three gates
were green when this shipped: 23,775 unit tests, a full CI run including a
real-Postgres smoke job, and a clean local gate suite. None of them starts the
server and waits. The 0.8.0 merge produced three defects — the scheduler
ownership break, the `smoke:export` seam assertion, and this — and they were
found by, respectively: reading the upstream diff, CI, and **running the app**.
No single check would have caught all three, and the cheapest of the three to
run was the one nearly skipped.

### Found by the Sunrise 0.8.0 merge (2026-08-05)

Both were found by merging `v0.8.0` (37 commits, `5964beb3..45e704d9`), and
neither is a defect in what Sunrise built — each is a **correct platform
decision whose fork-tier consequence has no seam**. Together they are the
strongest evidence yet for the pattern #480 and #525 circle: _a core change that
is right for core and silently wrong for a tier filling a documented seam_.

| #   | Ask                                                                                                                                                                                                                             | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Priority                                                                                                                                                                      | Issue                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 29  | **A per-user schedule has no owner after #502** — document `AiWorkflowSchedule.scope` as the supported carrier, or give the scheduler an explicit opt-in (`runAsCreator`, or a `scope` key core stamps into `execution.userId`) | #502 made scheduled runs system-owned (`userId: null`) because `AiWorkflowExecution.userId` is `onDelete: Cascade` and naming an operator meant erasing them destroyed the org's whole scheduled-run history. **Right for org-level cron rows, and it silently breaks per-user ones.** Resparkable creates one schedule row per user per workflow and `execution.userId` was the _only_ route by which a 04:30 run knew whose brain it was; after the merge every capability in all four background workflows throws `MissingResparkableUserError`. **Nothing failed loudly** — the type is `string \| null` so it type-checks, and 1,978 tier tests stayed green because they mock that boundary. The docs now state `userId: null` as a flat fact with no note that a per-user schedule needs a carrier | **Highest of the two** — total, silent, and reaches every fork with per-user schedules. The seam exists (`scope`); what is missing is that it is the answer, said anywhere    | [#532](https://github.com/human-centric-engineering/sunrise/issues/532) |
| 30  | **A fork cannot satisfy `export-sources.test.ts` from fork-owned code** — scope the schema scan to core-owned files, or let `lib/app/data-export.ts` declare its models                                                         | The guard reads every file in `prisma/schema/`, including `framework-*.prisma` and `app.prisma`, but builds `declared` from `SUBJECT_DATA_SOURCES` alone. So a fork that does exactly what #467 asks — fills `lib/app/data-export.ts`, writes its own completeness guard — **still fails a core test**, and the only way green is editing a Sunrise-owned file. Resparkable's 18 brain tables failed it on this merge. The irony is that the seam's own doc tells forks to write precisely the guard Resparkable wrote, then core's guard rejects the result                                                                                                                                                                                                                                              | Low effort. Same family as #480 (`defaults.test.ts`) and #525 (`registry.test.ts`), and the sharpest case: the other two miscount, this one **cannot be satisfied** correctly | [#533](https://github.com/human-centric-engineering/sunrise/issues/533) |

**Downstream status (#29):** fixed in the tier, no core edit. The owner now rides
on `AiWorkflowSchedule.scope` under the key `resparkableUserId`
(`repo/owner-scope.ts`), `requireResparkableUser` falls back to it when
`context.userId` is null, and `ensureResparkableSchedules` gained a third correction
that stamps rows written before 0.8.0 — without it, every install that upgrades
carries silently broken schedules for ever. `context.userId` still wins when both
are present, so the fallback can only fill a gap, never redirect a live session.

**Downstream status (#30):** the export is **built**, not deferred —
`collectResparkableSubjectData()` covers all 17 scoped tables (`ResparkableEmbedding` is
excluded as derived vectors, on the same grounds core excludes
`AiMessageEmbedding`), wired through `lib/app/data-export.ts` and guarded by the
tier's own schema-scanning test. **Two** local patches are carried, both removed
when [#533](https://github.com/human-centric-engineering/sunrise/issues/533)
lands: the 18 models in `export-sources.test.ts`'s `HANDLED_OUTSIDE_MANIFEST`,
and a pin in `scripts/smoke/export.ts`.

**The second was found by CI, not by the local gates, and that is the useful
part.** `scripts/smoke/export.ts` is **new in 0.8.0** and asserts
`Object.keys(bundle.app).length === 0` — "app seam is empty in vanilla Sunrise".
It is not a Vitest file, so a green `npx vitest run` says nothing about it; it
runs only in the real-Postgres smoke job. The local gate run was clean and the
PR still went red.

Two lessons worth keeping:

1. **A new `smoke:*` script in an upstream release is a gate the local suite
   does not cover.** The merge diff added `"smoke:export"` to `package.json`;
   that line should be read as "another thing to run", not as packaging noise.
   `npm run smoke:export` against the dev DB would have caught this before the
   push.
2. **This is the fourth instance of the same pattern** — after
   [#480](https://github.com/human-centric-engineering/sunrise/issues/480)
   (`defaults.test.ts`),
   [#525](https://github.com/human-centric-engineering/sunrise/issues/525)
   (`registry.test.ts`) and #533 (`export-sources.test.ts`) — and the sharpest
   evidence that the sweep #533 asks for is the right shape of fix. #480 closed
   as COMPLETED for exactly this mistake, and 0.8.0 then shipped a **brand-new**
   artifact making it again. A convention nobody can mechanically check gets
   re-broken by the next file. Reported on #533 rather than as a fifth issue.

The pin keeps the assertion's intent — the bundle must carry exactly one app key,
so a stray second collector still fails — and adds real-DB coverage the tier did
not have: the seventeen queries are unit-tested against a mocked client, which
cannot catch a column or table that does not exist.

**Both filed 2026-08-05**, after searching all 531 existing issues — neither was
a duplicate. #532 leans on [#386](https://github.com/human-centric-engineering/sunrise/issues/386)
(CLOSED), which is what makes the ask cheap: that issue already built the
schedule→execution `scope` stamping, so the carrier the fix needs exists and
nothing says it is the answer. #533 was filed as the third instance of the
#480/#525 pattern and asks for a sweep rather than a third one-off fix.

---

**Downstream status (#26):** Resparkable owns `components/resparkable/chat/resparkable-chat.tsx`.
It reuses the platform's `parseChatStreamEvent` and `getUserFacingError` — the
wire contract and the error map, both genuinely shared — and rebuilds only the
rendering. No Sunrise file is edited either way, so this ask is about saving the
_next_ fork the work, not about unblocking this one.

---

### Found building phase 7b — platform surfaces (2026-08-05)

Three seams, all the same shape: **core has a registry, core's own entries go in
a hardcoded list, and there is no `lib/app/*` seam for a fork's.** MCP tools
needed nothing (they are DB rows, and Resparkable shipped eight of them with no code
at all), which is exactly the contrast that makes the other three legible — the
extension point that was designed as data works for forks, and the three
designed as module-scoped maps do not.

None of the three is a defect. Each is a place where the fork story stops one
step short of where the platform's own docs say it goes.

| #   | Ask                                                                                                                                                                                               | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Priority                                                                                                                                                                          | Issue                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 32  | **A fork cannot register an MCP resource handler** — `lib/app/mcp.ts` + `registerAppMcpResourceHandler(type, handler)`, or read the handler name off `McpExposedResource.handlerConfig`           | `resource-registry.ts` dispatches on `row.resourceType` through `const HANDLERS`, a module-local map of core's four (`knowledge_search`, `pattern_detail`, `agent_list`, `workflow_list`). It is not exported and nothing merges into it, so a fork can insert an `McpExposedResource` row and the registry logs "no handler for type" and returns null. Everything _else_ about resources already works for forks — the URI pattern matcher, the templates, the auth context. Resparkable wanted `resparkable://today` and `resparkable://project/{slug}` as resources, which read paths **should** be: core's own doc says a resource is cheaper than a tool call because the client can read it without spending a turn. Without the seam those reads stay tools, which works and costs a turn each                                                                                                                                | Low effort — one exported registration function, mirroring `registerAppJob` / `registerAppCapability`. No trust boundary: the rows are still admin-created and still default-deny | [#540](https://github.com/human-centric-engineering/sunrise/issues/540) |
| 33  | **A fork cannot register an evaluation grader the batch worker can see** — `lib/app/evaluations.ts` + `registerAppGraders()`, called from `run-worker.ts` alongside its own barrel import         | `registerGrader` is exported and the registry is documented as pluggable, but it writes to a plain module-scoped `Map` filled by `graders/index.ts`. The worker runs in the **route** realm (`…/maintenance/tick`), which shares no module graph with `instrumentation.ts` — the split #462 was about — so a fork registering from `initApp()` fills a map the worker never reads, and the metric picker offers a grader that throws `No grader registered` at dispatch. There is no other point in the worker's import graph a fork can reach. Resparkable's `resparkable_triage_accuracy` is written, tested and unreachable from the UI because of it; it runs from a script instead                                                                                                                                                                                                                                               | Medium — the failure is a _late_ one (a queued run fails mid-drain, not at submit), and the registry's own header advertises pluggability that only holds for core                | [#541](https://github.com/human-centric-engineering/sunrise/issues/541) |
| 34  | **`AiApiKey.scopes` is a closed enum, so a fork cannot scope a key to its own surface** — make the scope list extensible (a fork-owned array merged into `VALID_SCOPES` and `createApiKeySchema`) | `createApiKeySchema` pins `z.enum(['chat','analytics','knowledge','webhook','admin'])` and `VALID_SCOPES` mirrors it, both in Sunrise-owned files. Meanwhile `withAuth` accepts a key of **any** scope, so today an iOS Shortcut posting to `/api/v1/resparkable/capture` works — with a key labelled `chat`. That is the whole problem: the narrow scope the plan wanted (`resparkable`) cannot be created, so the only key a user can mint for two-second capture is one that also reaches every other authenticated route. The seam is a one-line array; the alternative is every fork's self-service keys being broader than their purpose. **Confirmed still open at phase 9 (2026-08-13)** — `capture-channels.md` documents the gap for every capture channel; phase 9 did not attempt a workaround, deliberately, since the only way to unblock it from this tier would be to fork the two Sunrise-owned files this row names | Low effort. **States a real trust boundary** rather than a fork-convenience one: the gap makes least-privilege unavailable to forks, and the workaround is a wider key            | [#542](https://github.com/human-centric-engineering/sunrise/issues/542) |

**Downstream status (#32):** deferred, not worked around. Resparkable's read paths
stay MCP **tools** — `resparkable_search`, `resparkable_get_snapshot` and the rest are
all exposed and all work. The cost is one tool call where a resource read would
have been free, which is a performance note rather than a missing capability, so
there is nothing to carry locally and nothing for a host project to do.

**Downstream status (#33):** worked around, no core edit.
`registerResparkableGraders()` exists and is deliberately **not** called from
`initResparkable()` — registering into the wrong realm would be dead code that reads
like wiring. `scripts/framework/resparkable/eval-triage.ts` registers it in its own
process and dispatches through core's registry exactly as the worker would, so
the grader is exercised for real rather than only unit-tested. When the seam
lands, the change is one line and the script keeps working unchanged.

**Downstream status (#34):** no workaround exists, and the honest position is
that the Shortcut recipe in [`install.md`](./install.md) tells the operator to
mint a `chat`-scoped key and says plainly that it is wider than the job needs.
Resparkable cannot narrow it from the tier: the tier could _check_ a scope, but no
user can _create_ one to check. Nothing about capture is weakened — `withAuth`
still resolves the key to its owner and `OwnerScope` still confines every write
to that one brain — but the key on a phone reaches more than capture, and a
reader of `install.md` should know that before deciding to make one.

### Found restructuring the Resparkable nav (2026-08-05)

| #   | Ask                                                                                                                                                                                | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority                                                                                                                                                                   | Issue     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 35  | **No seam for the app shell's width** — the signed-in frame is `container mx-auto px-4`, hardcoded in three core files, and a fork that wants a full-bleed app must edit all three | `container` caps at the largest breakpoint and centres the remainder, which is right for marketing prose and wrong for an application with its own sidebar: on a 1990px display it spends ~450px on empty margins and puts the nav mid-screen. There is no prop, class or CSS variable to change it — `app/(protected)/layout.tsx`, `components/layouts/app-header.tsx` and `protected-footer.tsx` each hardcode it. Shape of the seam: a `.app-shell` utility in `globals.css` plus an `<AppHeader fullWidth>` prop, so `(public)` keeps the centred measure. This is a **layout** gap, not a theme one — `data-surface` (`.context/ui/surface-theming.md`) already classifies admin vs consumer, but it carries colour, not frame. | Medium                                                                                                                                                                     | Not filed |
| 36  | **`lib/app/data-transfer.ts`** — a transfer-policy seam mirroring `lib/app/data-export.ts`, plus the generic `lib/portability/**` engine upstream                                  | Art. 15 answers "what is this person owed?"; account portability answers "what moves, and what happens to every id that pointed at something?". The second needs foreign-key metadata Prisma's runtime `Prisma.dmmf` does not carry (it is pruned to `{name, kind, type, relationName}`), so it has to come from a generator at `prisma generate` time. Resparkable has built the whole thing — generator, model graph, policy vocabulary, coverage guard — in `lib/portability/**` with a fork-owned seam at `lib/app/data-transfer.ts`. Every part of it is app-agnostic; none of it is Resparkable-specific                                                                                                                       | Medium effort upstream, and it **subsumes #30**: the generated graph gives the coverage guard a cross-tier source of truth, which is the fix that ask is really asking for | Not filed |
| 37  | **`prisma/schema/base.prisma` should carry the `portability` generator block** — a fork cannot add a generator without editing a Sunrise-owned schema file                         | Resparkable declares `generator portability` in `framework-resparkable.prisma` because `base.prisma` is Sunrise-owned. That works, but it is the wrong home: the generator describes the _whole_ schema, not one tier. If upstream adopts #36 the block belongs in `base.prisma`, and the fork must delete its copy in the same change — two generators sharing a name is a hard `prisma generate` error, so this is a coordination point, not a silent conflict                                                                                                                                                                                                                                                                     | Low effort, but only meaningful alongside #36                                                                                                                              | Not filed |
| 38  | **The Art. 15 export API has no UI at all** — `GET /api/v1/users/me/export` is fully built, rate-limited and API-key-refusing, and nothing in `app/` or `components/` links to it  | A subject-access endpoint a person cannot reach is not a subject-access route; it is an endpoint an operator can curl. There is no "download my data" control on `/settings`, whose Account tab already has the Danger Zone that deletes it — so the destructive half of Art. 17 is one click away and the harmless half of Art. 15 is unreachable. Related: there is no `lib/app/settings-sections.ts` seam, so a fork cannot add the control without editing `components/settings/settings-tabs.tsx`                                                                                                                                                                                                                               | Low effort for the button; the settings seam is the reusable half, and mirrors `lib/app/admin-nav.ts`                                                                      | Not filed |

**Downstream status (#35): core edited, four files.** This is a deliberate
exception to the no-core-edits rule and the merge cost is real —
`app/globals.css` (a new `.app-shell` utility), `components/layouts/app-header.tsx`
(a `fullWidth` prop), `app/(protected)/layout.tsx` and
`components/layouts/protected-footer.tsx` (both switched to it). It could not be
done from the tier: the header and footer are rendered by the protected layout
_above_ `/resparkable`, so no Resparkable-owned file is in a position to widen them, and
a negative-margin full-bleed hack inside `/resparkable` would leave the header
centred above full-width content — visibly worse than either choice made
consistently. The edits are additive and each is one line at the call site, so
the upstream seam, if it lands in this shape, replaces them rather than
conflicting with them.

### Found by phase 29 (2026-08-19)

| #   | Ask                                                                                                                                                                                                                                    | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority                                                                                        | Issue     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------- |
| 39  | **`streaming-handler.ts`'s `done` event under-reports a turn's real cost**: three related gaps in `lib/orchestration/chat/streaming-handler.ts`, found reviewing Resparkable's new credit ledger, which debits from exactly this event | `buildDoneEvent`'s `costUsd` (~line 2701) reflects only the last tool-loop iteration's `usage`, not the turn's accumulated `turnCostUsd` (tracked separately, ~line 1638), so a turn that calls three tools is billed for one of the three. It also excludes `sideEffectModels`' own `costUsd` entirely (knowledge-base search, the rolling conversation summariser), attaching them as metadata only. Separately, several exit paths (`budget_exceeded_per_turn` mid-loop ~line 1721-1728, the monthly budget cap, `tool_loop_cap`, the catch-all `internal_error` path) `return` without ever yielding `done`, even when a `logCost()` call moments earlier already logged that turn's real spend, so that spend is never billed at all. Any fork metering chat spend off this event under-bills. Shape of the fix: fold `turnCostUsd` plus the sum of `sideEffectModels`' cost into `buildDoneEvent`, and make every exit path that already logged cost yield `done` first, matching what the sibling terminal-cap check (~line 1589-1627) already does correctly | Medium. No trust boundary crossed, but a real correctness bug for anyone billing off this event | Not filed |

**Downstream status (#39): documented, not worked around.** Resparkable's
phase 29 billing (`.context/framework/resparkable/phase-29-plan.md`) ships
anyway: v1 is "pretend billing," admin-grant funded, not real money, so
under-collection has no revenue impact yet. The gap and its fix shape are
recorded in that plan's §8d for whoever picks up the next billing phase, to
either carry a local workaround (e.g. Site A reading a cumulative-cost field
if core adds one) or wait for the upstream fix.

### Found by the Sunrise 0.9.0 merge (2026-08-19)

| #   | Ask                                                                                                                                                                                                                                                | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Priority                                                                                                                                                                                  | Issue     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 40  | **`check-sunrise-ancestry.sh` hardcodes `lib/sunrise-version.ts` / `SUNRISE_VERSION`, with no seam for a fork that renamed its version file** — env-var overrides (`VERSION_FILE`, `VERSION_CONST`), mirroring the existing `UPSTREAM_URL` pattern | New in 0.9.0 (#539), landed alongside `.github/workflows/fork-sync-integrity.yml`. It reads the version claim from a hardcoded path and constant name to confirm the fetched tag is genuinely Sunrise's release. Resparkable renamed that file to `lib/resparkable-version.ts` / `RESPARKABLE_VERSION` at the Obsiddy→Resparkable rebrand (`VERSIONING.md`), so on this fork the script always hits its own `skip` branch (`no SUNRISE_VERSION found`) and the squash-merge-ancestry guard never actually runs. Fails safe — a skip, not a false pass — but a guard that always skips is a guard that isn't there | Low — no trust boundary, and the failure mode is invisible rather than dangerous. Same shape as #480/#525/#533: a hardcoded assumption a fork's own rebrand breaks, with no per-fork seam | Not filed |

**Downstream status (#40):** fixed in the tier, no core edit. Two env vars —
`VERSION_FILE`, `VERSION_CONST` — added to the script with defaults matching
Sunrise's own names, so behaviour is unchanged for Sunrise itself and for any
fork that didn't rename the file; `fork-sync-integrity.yml` sets them to
`lib/resparkable-version.ts` / `RESPARKABLE_VERSION` for this repo. Verified
against `tests/unit/scripts/ci/check-sunrise-ancestry.test.ts` (13/13 still
green — the suite exercises the unset-default path). Remove the two env-var
lines from the workflow if #539's file/constant names ever stop being
hand-editable per fork.

`scripts/ci/check-changelog.ts` and `scripts/ci/changelog-structure.ts` (also
new in 0.9.0) hit the same shape — a direct `@/lib/sunrise-version` import,
not a shell-parsed path, so no env-var seam is possible there. Edited both
directly, plus the message text `changelog-structure.ts` prints on a version
mismatch, plus the two tests asserting that text
(`tests/unit/scripts/ci/check-changelog.test.ts`,
`tests/unit/scripts/ci/changelog-structure.test.ts`). Same root cause as the
ancestry script; not filed as a separate row.

**A related but distinct rename bit a new test too, unrelated to #539.**
`lib/orchestration/capabilities/dispatcher.ts`'s `globalThis` singleton key
is `resparkableCapabilityDispatcher` here, not upstream's
`sunriseCapabilityDispatcher` — a pre-existing local edit to a Sunrise-owned
file that predates this merge (not introduced by it, and not otherwise
tracked in this file). The new
`tests/unit/lib/orchestration/engine/executors/tool-call-cold-registry.test.ts`
(#537's regression test) deletes the upstream key name to force a cold
process; on this fork that delete is a silent no-op, the real singleton
survives across the file's two tests, and the second test's own precondition
assertion (`capabilityDispatcher.has(...)` false) catches it. Fixed by
pointing the test's `delete` at the fork's actual key, with a comment
explaining why. Worth a closer look before the next merge: `git grep
"globalFor.*globalThis as unknown as"` finds at least one more renamed key
(`lib/orchestration/chat/context-builder.ts`), and any new upstream test
written against the un-renamed original will fail the same way.

---

## State at the Sunrise 0.9.0 merge (2026-08-19)

Checked with the two commands §6 of [The process](#the-process) prescribes, run
together: `git fetch upstream` (61 commits since the 0.8.0 merge point,
`45e704d9..v0.9.0` is 45 of them — the rest are past the tag, on
`upstream/main`, and not part of this merge) and
`gh issue list --state all --limit 400`.

**Eight asks landed in 0.9.0** — #506, #507, #508, #509, #510 (the 2026-08-02
gate run) and #528, #534, #537 (phases 6b/7 and the running-app find) — see
[Landed](#landed) below for what changed here. The `sanitize.ts` tab/LF/CR
patch (#506) and the `registry.test.ts` capabilities-count stub (#525's
workaround, unaffected) are the two local patches this file has tracked the
longest; #506's is now removed, verbatim upstream's version.

**Four more closed upstream but are NOT in this merge.** #526, #532, #535 and
#536 are closed on GitHub, but their fixing commits (`f781858a`, `e9dc4a7d`)
sit in the 16-commit range **after** the `v0.9.0` tag, on `upstream/main`
un-tagged. Closed-on-GitHub is not landed-in-a-release — treat all four as
still open until the next sync actually contains them. This is exactly the
staleness trap §6 warns about, from the other direction: a closed issue with
a real fix commit still reads as pending here if that commit is not an
ancestor of what got merged.

**Still open, no local workaround needed:** #525, #533 (both the
`lib/app/*`-seam-vs-core-test family — low-friction, no fork blocked), #540,
#541 (both deferred, not worked around — see their downstream-status notes
above), #542 (no workaround possible from the tier).

**One new ask filed by this merge:** #40 — `check-sunrise-ancestry.sh` and
`check-changelog.ts` both hardcode `lib/sunrise-version.ts` /
`SUNRISE_VERSION`, which this fork renamed. Fixed locally (env-var overrides
for the shell script; a direct import-path edit for the two TypeScript
files, which cannot take an env-var seam the same way) — see its row above.

---

## State at the Sunrise 0.8.0 merge (2026-08-05)

Checked with the two commands §6 of [The process](#the-process) prescribes, run
together: `git fetch upstream` (37 commits, `5964beb3..45e704d9`, tag `v0.8.0`)
and `gh issue list --state all --limit 400`.

**No Resparkable ask landed in 0.8.0.** All eight remain open — #506, #507, #508,
#509, #510 (the 2026-08-02 gate run), #525 (phase 6b), #526 (phase 6c) and #528
(phase 7) — so **both local patches are still required** and stay: the
tab/LF/CR strip in `lib/security/sanitize.ts` (#506) and the
`initAppCapabilities` stub in `registry.test.ts` (#525). Ask #30 adds a third.

That is not a complaint about responsiveness: 0.8.0 is a large release whose
security half was already in flight when these were filed, and eleven earlier
Resparkable asks landed in the three days before it. The point of recording it is
the inverse of the staleness trap §6 describes — **this file must not say
"landed" for work that has not**, and after a release tag the pull to assume
otherwise is strongest.

What the release _did_ change for this fork is two things nobody asked for and
neither of which is a bug: #502's system-owned runs (ask #29) and #467's export
seam (ask #30). Both are above.

---

## Landed

**Eight asks landed in `v0.9.0`**, merged into this fork on 2026-08-19
(`45e704d9..v0.9.0`, 45 commits).

| #   | Ask                                                                            | Landed in   | What it changed here                                                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 20  | Open redirect: `isRootRelativePath` tab/LF/CR bypass (#506)                    | in `v0.9.0` | `lib/security/sanitize.ts` took upstream's `normalizeRootRelativePath()` + `isRootRelativePath()`/`safeCallbackUrl()` wholesale, replacing the local patch. The local `FORK NOTE` comment is gone with it — the fix landed exactly as filed                                                      |
| 21  | Domain-separate storage-access and approval tokens (#507)                      | in `v0.9.0` | Both `lib/storage/access-tokens.ts` and `lib/orchestration/approval-tokens.ts` now sign a `typ` field into the payload and check it on verify. No fork edit needed — the fork carried no local patch, only the ask                                                                               |
| 22  | `LocalProvider.deletePrefix` root-equivalent prefix (#508)                     | in `v0.9.0` | `deletePrefix` now scopes through the same guard `upload`/`delete`/`download` use; a root-equivalent prefix is refused rather than recursively deleting the storage root                                                                                                                         |
| 23  | `functionDefinition.name === slug` on capabilities (#509)                      | in `v0.9.0` | `lib/validations/orchestration.ts` gained the cross-field `.refine()`; a capability whose advertised tool name diverges from its dispatch slug is now rejected at write time                                                                                                                     |
| 24  | Prisma format check folded into `validate` (#510)                              | in `v0.9.0` | `npm run validate` now runs `format:prisma:check`; confirmed clean against `prisma/schema` (13 files) on this merge                                                                                                                                                                              |
| 27  | `CAPABILITY_BINDING_MODE=strict` breaks every workflow `tool_call` step (#528) | in `v0.9.0` | Workflow-shaped `agentId`s (`workflow:<cuid>`) are handled explicitly rather than falling through to a binding lookup that can never exist for a non-`AiAgent` id                                                                                                                                |
| 6   | `safe-url.ts` names a compensating control that doesn't compensate (#534)      | in `v0.9.0` | `checkSafeProviderUrl` no longer over-claims what it blocks; the doc correction landed alongside a wider SSRF sweep (redirect re-validation, IPv4-mapped IPv6 denylist entries) that the ask's narrower finding sat inside                                                                       |
| 31  | `tool-call.ts` dispatches without ensuring capability registration (#537)      | in `v0.9.0` | `executors/tool-call.ts` now imports and calls `registerBuiltInCapabilities()` before dispatch, matching `agent-call.ts`. The tier's own `initResparkable()` workaround (`scaffold.test.ts` regression test) is now redundant — harmless to leave, safe to remove next time that file is touched |

**Four asks are closed upstream but NOT in this merge** — their fixing
commits are past the `v0.9.0` tag, on unreleased `upstream/main`: #526
(`ChatInterface` `streamEndpoint` prop, `f781858a`), #532, #535, #536 (three
doc corrections, `e9dc4a7d`). Still tracked as open above until a sync
actually contains them.

---

**Eleven asks landed upstream between 2026-07-29 and 2026-07-31**, merged into
this fork on 2026-07-31 (`upstream/main` at `5964beb3`, 90 commits). All of it
is on Sunrise `main` and unreleased — `0.7.0` is the last tag and predates every
row below, so "Landed in" names the commit, not a version. **Everything below
shipped in `v0.8.0`**, which is the first tag to contain any of it.

Two of them changed how Resparkable is wired rather than merely what it depends on,
and the first is the reason this fork's headline claim is finally true:
**Resparkable touches zero Sunrise-owned files.**

| #   | Ask                                                | Landed in                            | What it changed here                                                                                                                                                                                                                                                                                                                                                                     |
| --- | -------------------------------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2   | Protected-nav seam (#473)                          | `c5cc9fc2` + `255dbc6e` + `1da8f68f` | `components/layouts/protected-nav.tsx` reverted to pristine. Resparkable's link is now `RESPARKABLE_NAV_ITEM` in `lib/framework/resparkable/protected-nav.ts`, spread by the host in `lib/app/protected-nav.ts`. **The last core-file edit is gone.** Also landed `lib/app/auth-landing.ts` — the post-auth destination half — which Resparkable leaves `null`, deliberately (see below) |
| 1   | Recurring-job seam (#469)                          | `5d6d3a9e`                           | Shipped as **`lib/app/jobs.ts`** + `registerAppJob({ name, intervalMs, run })`, **not** `lib/app/maintenance-tasks.ts` / `registerAppMaintenanceTask` as `plan.md` assumed. Phase 7 uses the real name. `intervalMs` is a minimum gap, last-run times are in process memory, so a multi-instance deploy runs each job about once per instance — write the sweeps idempotent              |
| —   | Turbopack registry loss (#462, not our ask)        | `93a8180e`                           | The phase-6 blocker. Contributor and dispatcher registries are `globalThis`-backed, so phase 6's 14 capabilities and its context contributor survive the instrumentation/route module split. No fork-side self-heal needed                                                                                                                                                               |
| 3   | Fork-tolerant `defaults.test.ts`                   | `95192a84` (#480)                    | Now a `SEAM_DEFAULTS` table where a fork **pins** the value it fills instead of deleting the row — the fork-owned-manifest option we argued for. Seven rows pinned here, plus one added for `lib/app/leaf-bootstrap.ts`, which the new drift guard would otherwise flag as unprotected                                                                                                   |
| 10  | `generatedColumnExists()` in `lib/db/drift-probes` | `75ee421b` (#481)                    | Local copy deleted; `lib/framework/resparkable/db-drift.ts` imports it. Core's own A1 probe was switched to it too, so the blind spot the copy existed to close is closed upstream as well                                                                                                                                                                                               |
| 16  | Table-agnostic `assertStoredVectorDimensions()`    | `a0931492` (#491)                    | ~40 duplicated lines in `repo/embeddings.ts` replaced by the shared guard, with the fork supplying owner-scoped closures — which is what keeps one user's mismatch from throwing for everybody. The fork's `logger.error` is kept via catch/rethrow: a thrown string is not queryable                                                                                                    |
| 15  | Storage private objects + read path                | `e1b201cb` → `075b1450` (#490)       | **A correctness fix, not just a cleanup.** `resolveRetentionCapability()` named `local` and refused it; the local provider now has a private root and a signed read route, so the name check would have wrongly refused a capable provider _and_ trusted any future one that isn't `local`. It now reads `getStorageCapabilities()`, where undeclared means "cannot"                     |
| 14  | Default `Cache-Control` on `/api/v1/**`            | `99289aa0` (#487)                    | `successResponse()` and `checkConditional()`'s 304 both send `private, no-cache`. `lib/framework/resparkable/api/cache.ts` shrank from three exports to one constant, kept only for the board export's raw `Response`, which never passes through the envelope helpers                                                                                                                   |
| 18  | `apiClient.put()`                                  | `5bf36168` (#495)                    | `PATCH /resparkable/tasks/[id]/tags` → **`PUT`**. Behaviour never differed; the verb did, and only because adding one to the client would have been a core-file edit                                                                                                                                                                                                                     |
| 12  | `framework:*` script namespace                     | `518c023e` (#483)                    | Docs-only upstream. Resparkable had already moved to `framework:resparkable:*` under `scripts/framework/` in phase 2; this makes that the documented convention rather than a guess                                                                                                                                                                                                      |
| 11  | `prisma format` on `orchestration-agents.prisma`   | `0a84ae2c` (#482)                    | Whitespace, plus a CI gate (`prisma format` then `git diff --exit-code`). A fork's first `prisma format` no longer dirties a core file                                                                                                                                                                                                                                                   |
| 19  | ESLint ignores `coverage/`                         | `b073672a` (#497)                    | `npm run lint` after a coverage run no longer dies on `ENOENT` from the stale `--cache`                                                                                                                                                                                                                                                                                                  |

**`lib/app/auth-landing.ts` is deliberately left `null`.** #473's other half lets
a fork send users to `/resparkable` after login instead of `/dashboard`, and the
argument we made upstream was that the dead-end destination is the half that
actually bites. It is still the right seam — but _which_ route a host lands on is
a host's product decision, not something a framework tier should force on every
install. This repo is Resparkable's development home, not a product built on it, so
it keeps the platform default. `install.md` §2.11 tells a host how to set it.
